name: example-knowledge
schema: meta/frontmatter.schema.json
rulesets: meta/rulesets.yaml
include: ["**/*.md"]
exclude: ["meta/conventions.md", "meta/templates/**"]
requires:
  status: [active]
  provenance: [verified, decided]
  visibility: { not: [restricted] }