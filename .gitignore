# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binaries and coverage
*.test
*.out
coverage.html

# Go
go.work
go.work.sum
vendor/

# Build & release artifacts
build/
dist/
completions/
manpages/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.*

# Claude Code. Personal settings stay out; what the repository shares comes in.
# settings.json is where qm init writes the session hook, and that placement is
# only worth anything because git carries the file to every worktree and clone.
.claude/*
!.claude/skills/
!.claude/settings.json
CLAUDE.md